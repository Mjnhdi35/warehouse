{
  "name": "warehouse",
  "packageManager": "yarn@4.10.3",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "setup:yarn": "corepack enable && yarn install",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{js,jsx,ts,tsx,json,css,md}\"",
    "tsc": "yarn nx run-many -t tsc",
    "lint": "yarn nx run-many -t lint",
    "build": "yarn nx run-many -t build",
    "build:api": "yarn nx build api",
    "start:api": "yarn nx start:prod api",
    "build:web": "yarn nx build web",
    "start:web": "yarn nx start web"
  },
  "engines": {
    "node": "22.16.0",
    "yarn": "4.10.3"
  },
  "devDependencies": {
    "nx": "22.0.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
