services:
  - type: web
    name: warehouse-api
    runtime: node
    plan: free
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api
    buildCommand: |
      corepack enable || true &&
      corepack prepare yarn@4.10.3 --activate || true && 
      yarn install --immutable &&
      yarn workspace @warehouse/api build
    startCommand: yarn workspace @warehouse/api start

  - type: web
    name: warehouse-web
    runtime: node
    plan: free
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    buildCommand: |
      corepack enable || true && 
      corepack prepare yarn@4.10.3 --activate || true && 
      yarn install --immutable && 
      yarn workspace @warehouse/web build
    startCommand: yarn workspace @warehouse/web start
